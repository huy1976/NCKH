# Thuật toán train mô hình AI 1D-CNN + Autoencoder

Kho lưu trữ này chứa mã nguồn triển khai mô hình học sâu kết hợp giữa **1D-CNN Autoencoder** và **Classifier** để phân loại và xử lý tín hiệu.

## 📂 Dữ liệu (Dataset)
Bạn có thể tải hoặc đưa tập dữ liệu (dataset) dùng cho mô hình vào đường dẫn sau:
👉 **[Link để bỏ file dataset vào đây]** *(Thay thế bằng đường dẫn tới thư mục chứa dữ liệu của bạn, ví dụ: Google Drive hoặc thư mục `data/`)*

## 📊 Kết quả huấn luyện (Training Results)
Biểu đồ và kết quả đánh giá quá trình train mô hình:

![Kết quả train mô hình](./đường_dẫn_hoặc_tên_file_ảnh_kết_quả.png)
*(Bạn có thể thay thế đoạn trong ngoặc vuông bằng đường dẫn tới file ảnh kết quả của bạn, ví dụ: `training_history_optimized.png`)*
